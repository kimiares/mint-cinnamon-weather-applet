Job scraper

Files:
- scrape_jobs.py  -- standalone scraper script
- requirements.txt -- Python dependencies

Run:
  pip install -r jobs/requirements.txt
  python3 jobs/scrape_jobs.py --query ".NET"

This folder was added as part of the requested job-search implementation. No git commits were made; please confirm if a branch and commits are desired.
